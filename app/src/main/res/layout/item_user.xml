<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="@dimen/padding"
    android:background="@drawable/film_rounded_corner"
    android:gravity="center_vertical"
    android:layout_marginBottom="@dimen/marginBottom_2">

    <ImageView
        android:id="@+id/contactIcon"
        android:layout_width="@dimen/img_width_for_item_user"
        android:layout_height="@dimen/img_height_for_item_user"
        android:layout_marginEnd="@dimen/marginEnd"
        android:src="@drawable/contact"
        android:contentDescription="@string/contact_icon_description"
        app:tint="@color/transparent_sky" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="@dimen/paddingStart">

        <TextView
            android:id="@+id/textViewName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/textSize_2"
            android:textColor="@color/transparent_sky"
            android:fontFamily="@font/gg_sans_bold"
            android:layout_marginBottom="@dimen/marginBottom_2"/>

        <TextView
            android:id="@+id/textViewPhoneNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/textSize"
            android:textColor="@color/black"/>

    </LinearLayout>

</LinearLayout>
